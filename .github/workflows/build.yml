name: Lupra Prime Garantili

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Java Kurulumu
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Flutter Kurulumu
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Uygulamayı Oluştur ve Derle
        run: |
          # 1. Çakışmayı önlemek için yeni bir klasör oluşturuyoruz
          flutter create mobil_uygulama --org com.lupraprime
          
          # 2. O klasörün içine giriyoruz (Artık her şey burada yapılacak)
          cd mobil_uygulama
          
          # 3. Webview paketini ekle
          flutter pub add webview_flutter
          
          # 4. Kodları yaz (Eski dosyayı silip yenisini oluşturuyoruz)
          rm lib/main.dart
          cat > lib/main.dart <<EOF
          import 'package:flutter/material.dart';
          import 'package:webview_flutter/webview_flutter.dart';

          void main() {
            runApp(const LupraPrimeApp());
          }

          class LupraPrimeApp extends StatelessWidget {
            const LupraPrimeApp({super.key});

            @override
            Widget build(BuildContext context) {
              return MaterialApp(
                debugShowCheckedModeBanner: false,
                title: 'Lupra Prime',
                theme: ThemeData(
                  primarySwatch: Colors.blue,
                  scaffoldBackgroundColor: Colors.black, 
                ),
                home: const WebViewScreen(),
              );
            }
          }

          class WebViewScreen extends StatefulWidget {
            const WebViewScreen({super.key});

            @override
            State<WebViewScreen> createState() => _WebViewScreenState();
          }

          class _WebViewScreenState extends State<WebViewScreen> {
            late final WebViewController _controller;

            @override
            void initState() {
              super.initState();
              _controller = WebViewController()
                ..setJavaScriptMode(JavaScriptMode.unrestricted)
                ..setBackgroundColor(const Color(0x00000000))
                ..setNavigationDelegate(
                  NavigationDelegate(
                    onNavigationRequest: (NavigationRequest request) {
                      return NavigationDecision.navigate;
                    },
                  ),
                )
                ..loadRequest(Uri.parse('https://lupraprime.blogspot.com/'));
            }

            Future<bool> _onWillPop() async {
              if (await _controller.canGoBack()) {
                await _controller.goBack();
                return false;
              }
              return true;
            }

            @override
            Widget build(BuildContext context) {
              return WillPopScope(
                onWillPop: _onWillPop,
                child: Scaffold(
                  body: SafeArea(
                    child: WebViewWidget(controller: _controller),
                  ),
                ),
              );
            }
          }
          EOF
          
          # 5. İNTERNET İZNİ (Manifest dosyasını bulup düzenle)
          sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
          
          # 6. Bağımlılıkları güncelle ve APK üret
          flutter pub get
          flutter build apk --release --no-tree-shake-icons

      - name: APK İndirilebilir Yap
        uses: actions/upload-artifact@v4
        with:
          name: lupra-prime-apk
          # APK artık alt klasörün içinde oluştuğu için yolu güncelledik:
          path: mobil_uygulama/build/app/outputs/flutter-apk/app-release.apk
