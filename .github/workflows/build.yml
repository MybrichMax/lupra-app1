name: Lupra Prime (Akıllı Logo)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Java Kurulumu
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Flutter Kurulumu
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Uygulamayi Hazirla ve Hatalari Duzelt
        run: |
          # 1. Proje Klasörünü Oluştur
          flutter create mobil_uygulama --org com.lupraprime
          
          # 2. LOGO KONTROLÜ VE DÜZELTME (Akıllı Kısım)
          # İsminde 'logo' geçen herhangi bir PNG dosyasını bul (Büyük/küçük harf farketmez)
          find . -maxdepth 1 -iname "*logo*.png" -exec cp {} mobil_uygulama/logo.png \; -quit
          
          # Eğer logo hala yoksa (Kullanıcı yüklemeyi unuttuysa), geçici bir ikon indir
          if [ ! -f mobil_uygulama/logo.png ]; then
             echo "UYARI: Kendi logonuz bulunamadı. Geçici ikon kullanılıyor."
             wget -O mobil_uygulama/logo.png https://cdn-icons-png.flaticon.com/512/777/777242.png
          fi
          
          # 3. Proje klasörüne gir
          cd mobil_uygulama
          
          # 4. Gerekli Paketleri Ekle
          flutter pub add webview_flutter
          flutter pub add -d flutter_launcher_icons
          
          # 5. İKON YAPILANDIRMASI
          cat > flutter_launcher_icons.yaml <<EOF
          flutter_launcher_icons:
            android: true
            ios: true
            image_path: "logo.png"
            min_sdk_android: 21
            adaptive_icon_background: "#000000" 
            adaptive_icon_foreground: "logo.png"
          EOF
          
          # 6. KODLARI YAZ
          rm lib/main.dart
          cat > lib/main.dart <<EOF
          import 'package:flutter/material.dart';
          import 'package:webview_flutter/webview_flutter.dart';

          void main() {
            runApp(const LupraPrimeApp());
          }

          class LupraPrimeApp extends StatelessWidget {
            const LupraPrimeApp({super.key});

            @override
            Widget build(BuildContext context) {
              return MaterialApp(
                debugShowCheckedModeBanner: false,
                title: 'Lupra Prime',
                theme: ThemeData(
                  primarySwatch: Colors.blue,
                  scaffoldBackgroundColor: Colors.black, 
                ),
                home: const WebViewScreen(),
              );
            }
          }

          class WebViewScreen extends StatefulWidget {
            const WebViewScreen({super.key});

            @override
            State<WebViewScreen> createState() => _WebViewScreenState();
          }

          class _WebViewScreenState extends State<WebViewScreen> {
            late final WebViewController _controller;

            @override
            void initState() {
              super.initState();
              _controller = WebViewController()
                ..setJavaScriptMode(JavaScriptMode.unrestricted)
                ..setBackgroundColor(const Color(0x00000000))
                ..setNavigationDelegate(
                  NavigationDelegate(
                    onNavigationRequest: (NavigationRequest request) {
                      return NavigationDecision.navigate;
                    },
                  ),
                )
                ..loadRequest(Uri.parse('https://lupraprime.blogspot.com'));
            }

            Future<bool> _onWillPop() async {
              if (await _controller.canGoBack()) {
                await _controller.goBack();
                return false;
              }
              return true;
            }

            @override
            Widget build(BuildContext context) {
              return WillPopScope(
                onWillPop: _onWillPop,
                child: Scaffold(
                  body: SafeArea(
                    child: WebViewWidget(controller: _controller),
                  ),
                ),
              );
            }
          }
          EOF
          
          # 7. UYGULAMA İSMİNİ "Lupra Prime" YAP
          sed -i 's/android:label="mobil_uygulama"/android:label="Lupra Prime"/g' android/app/src/main/AndroidManifest.xml
          
          # 8. İNTERNET İZNİ
          sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
          
          # 9. Derle
          flutter pub get
          dart run flutter_launcher_icons
          flutter build apk --release --no-tree-shake-icons

      - name: Yeni APK'yı İndir
        uses: actions/upload-artifact@v4
        with:
          name: lupra-prime-v2
          path: mobil_uygulama/build/app/outputs/flutter-apk/app-release.apk
